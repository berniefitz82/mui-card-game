import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#386641',
      dark: '#27472d',
    },
    secondary: {
      main: '#bc4749',
    },
    background: '#27472d',
  },
});

export default theme;
